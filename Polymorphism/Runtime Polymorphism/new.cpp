//If the function exist in many forms and the resolution of the function call is done at runtime than runtime polymorphism is achieved
//It can be achieved through virtual function

/*Pure Polymorphism
    same as virtual function
*/